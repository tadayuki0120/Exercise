<% if @book.errors.any? %>
<div class=error_explanation>
  <div class="error_count"><%= @book.errors.count %>errors prohibited this obj from being saved:</div>
  <ul class=error_list>
    <% @book.errors.full_messages.each do |message| %>
    <li class=error_message><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>